import React from "react";

const CompetitionSelect = () => (
  <select className="form-control" id="competitionSelect">
    <option value="" disabled selected>
      Select A Competition
    </option>
  </select>
);

export default CompetitionSelect;
